extends ../../layout.pug
block contenido
    h1=imagen.title
    img(src="/static/imagenes/"+imagen._id+"."+imagen.extension, alt="imagenes")
    if(typeof imagen.creator != "undefined")
        p="El creador de la iamge es "+ imagen.creator.email