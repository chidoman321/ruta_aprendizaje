extends ../../layout.pug
block contenido
    div(class="container ")
        h1 Mis Imágenes
        for imagen in i
            .white.border.tr.large-padding
                .row
                    .col-md-10
                        h2=imagen.title
                    -col-md-2   
                        a(href="/app/imagenes/"+imagen._id+"/edit" class="right-space") Editar
                        form(action="imagenes/"+imagen._id+"?_method=DELETE" class="link" method="POST")
                            input(type="submit" class="right-space" value="Eliminar")